package store

import (
	"testing"
)

func Test_CreateChannel_Failure(t *testing.T) {
	//ctrl, ctx := gomock.WithContext(context.Background(), t)
	//m := mock.NewMockChannelDatabase(ctrl)
	//
	//m.EXPECT().Create(ctx, 1).Return(1)
	//m.EXPECT().Sum(1, 2).Return(3).MinTimes(1)
}
